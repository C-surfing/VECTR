# 环境变量配置示例
# 复制此文件为 .env.local 并填入实际值

GEMINI_API_KEY=your_gemini_api_key_here
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
VITE_ADMIN_PASSWORD=your_admin_password_here

# 媒体上传 provider：
# - supabase（默认，现有方案）
# - supabase-proxy（推荐：走 /api/supabase-upload，由服务端写入 Supabase Storage，更稳）
# - cos-proxy（走 /api/media-upload 上传到腾讯云 COS）
# - local（纯本地 DataURL，不依赖云端，适合离线写作）
VITE_MEDIA_UPLOAD_PROVIDER=supabase
VITE_MEDIA_UPLOAD_API=/api/media-upload
VITE_SUPABASE_UPLOAD_API=/api/supabase-upload
VITE_MEDIA_LOCAL_FALLBACK=true

# Supabase Storage 代理上传（仅后端使用，不要加 VITE_ 前缀）
# 保留 SUPABASE_SERVICE_ROLE_KEY 在服务端，不要暴露到前端
SUPABASE_URL=your_supabase_url_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
SUPABASE_MEDIA_BUCKET=media

# 数据 provider：
# - supabase（默认）
# - domestic-api（走 /api/domestic/*，连接国内 MySQL）
# - local（纯本地 localStorage，发布后自己可见）
VITE_DATA_PROVIDER=supabase
VITE_DATA_API_BASE=/api/domestic

# 访客认证 provider：
# - supabase（默认）
# - domestic-api（走 /api/domestic/auth）
# 管理员登录与熊猫头像不受影响，仍使用本地管理员密码 + Supabase 熊猫资源
VITE_AUTH_PROVIDER=supabase
VITE_AUTH_API_BASE=/api/domestic

# 腾讯云 COS（仅后端使用，不要加 VITE_ 前缀）
COS_SECRET_ID=your_cos_secret_id
COS_SECRET_KEY=your_cos_secret_key
COS_BUCKET=your_bucket_name
COS_REGION=ap-guangzhou
# 可选：绑定 CDN/自定义域名后填入，末尾不带 /
COS_PUBLIC_BASE_URL=https://your-media-domain.example.com

# 国内 MySQL（仅后端 API 使用，不要加 VITE_ 前缀）
DOMESTIC_DB_HOST=127.0.0.1
DOMESTIC_DB_PORT=3306
DOMESTIC_DB_USER=root
DOMESTIC_DB_PASSWORD=your_db_password
DOMESTIC_DB_NAME=vectr
